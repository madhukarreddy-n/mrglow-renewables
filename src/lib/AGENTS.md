# Shared libraries

- `brand.ts` — locked company identity (do not invent contact or stats).
- `bom.ts` — BOM line GST math and estimate numbers (`EST-001-V1`).
- `proposal-pdf.ts` — staff PDF for Mr.GLOW RENEWABLES (no invented bank or street address).
- `storage-cleanup.ts` — overwrite one PDF per proposal; delete lead folders in Storage.
- `supabase/` — browser, server (anon + cookies), service role.
- `workflow.ts` — lead status enum and allowed transitions.
- `email/send.ts` — Resend, then Brevo; skip and log if neither key is set.
- `calculator/engine.ts` — savings math only.
