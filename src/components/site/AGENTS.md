# Site chrome (header, footer, logo)

- **Header:** circular crop of `public/brand/logo-lockup.jpg` + `BrandWordmark` (`light` on navy).
- **Footer / About:** `Logo variant="lockup"` (full stacked artwork on white).
- **`GlowO`:** same type size as G/L/W (`~1em`). Letter **O** (not a tiny SVG circle). Orange bolt at forward ~45° (`rotate(48)`). Bolt **masks/cuts** the O so the ring is broken where the energy edges pass, as on the lockup. GLOW is one colour (`text-lime`); `Mr.` follows header/footer contrast.
- Nav label: “Why Mr.GLOW”. Do not revert to “MR GLOW Energy Resources”.
